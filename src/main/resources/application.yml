server:
  port: 9094
  servlet:
    context-path: /LoadFile

spring:
  application:
    name: LoadFile
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.SQLServerDialect
  jmx:
    enabled: false

  datasource:
    url: jdbc:sqlserver://192.168.0.39;database=LoadFileDev;encrypt=false;
    username: sa
    password: Makron3254!@

  kafka:
    request:
      timeout:
        ms: 600000
    bootstrap-servers: certaconmsfileuploadprod.servicebus.windows.net:9093
    client-id: organizer-service
    properties:
      sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="$ConnectionString" password="Endpoint=sb://certaconmsfileuploadprod.servicebus.windows.net/;SharedAccessKeyName=SendReceiveOnly;SharedAccessKey=/EGltUOU9EG9HD9S8yauegZbuL+H7vUdi+AEhBtRYLE=";
      sasl.mechanism: PLAIN
      security.protocol: SASL_SSL
    consumer:
      auto-offset-reset: earliest
      group-id: $Default
      listener:
        concurrency: 1
      properties:
        spring.json:
          use.type.headers: false
          value.default.type: br.com.certacon.certabotloadfiles.kafka.consumer.UploadMesssage
      enable-auto-commit: false
    listener:
      ack-mode: manual_immediate

topic:
  uploadcertabot: topic-upload-certabot



config:
  downloadPath: /home/selenium/Downloads
  archivedFilesDir: /tmp/ARQUIVADOS/
  sentFilesDir: /tmp/ENVIADOS/
  rootDir: /tmp/
  restBuilder: http://192.168.0.39:9154/CertaBotOrganize
